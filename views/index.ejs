<%- include('base/header'); %>
<%- include('base/navbar', { user: currentUser }); %>

<%- include('components/heading', { user: currentUser }); %>

<div class="jumbotron">
  <h1 class="text-center mb-5">Latest posts</h1>
  <ul>
    <% messages.forEach(message => { %>
      <%- include('components/card', { message, user: currentUser }) %>
    <% }); %>
  </ul>
</div>

<%- include('base/footer'); %>
